<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Obrigada! Sua transformação começou</title>
  <meta name="description" content="Obrigada por se inscrever na Mentoria Seja Livre. Confira seu email para o próximo passo.">
</head>
<body>

<div class="min-h-screen bg-gradient-to-br from-blue-50 to-purple-50 flex items-center justify-center p-4">
  <div class="max-w-2xl w-full bg-white rounded-lg shadow-xl p-8 text-center">
    
    <!-- Success Icon -->
    <div class="mx-auto w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mb-6">
      <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
    </div>

    <!-- Main Message -->
    <h1 class="text-3xl font-bold text-gray-900 mb-4">
      Obrigada! Sua jornada de transformação começou 🎉
    </h1>
    
    <p class="text-lg text-gray-600 mb-8">
      Recebemos suas informações e já estamos analisando seu perfil para a Mentoria Seja Livre.
    </p>

    <!-- Progress Bar -->
    <div class="mb-8">
      <div class="flex items-center justify-between text-sm text-gray-600 mb-2">
        <span>Progresso da inscrição</span>
        <span id="progress-percentage">80% completo</span>
      </div>
      <div class="w-full bg-gray-200 rounded-full h-3">
        <div id="progress-bar" class="bg-gradient-to-r from-blue-500 to-purple-600 h-3 rounded-full transition-all duration-1000" style="width: 80%"></div>
      </div>
    </div>

    <!-- Next Steps -->
    <div class="bg-blue-50 rounded-lg p-6 mb-8 text-left">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">Próximos passos:</h2>
      
      <div class="space-y-4">
        <div class="flex items-start">
          <div class="flex-shrink-0 w-8 h-8 bg-blue-500 text-white rounded-full flex items-center justify-center text-sm font-medium mr-3">1</div>
          <div>
            <h3 class="font-medium text-gray-900">Confira seu email (chegará em alguns minutos)</h3>
            <p class="text-gray-600 text-sm">Você receberá um questionário detalhado para completarmos sua análise</p>
          </div>
        </div>
        
        <div class="flex items-start">
          <div class="flex-shrink-0 w-8 h-8 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center text-sm font-medium mr-3">2</div>
          <div>
            <h3 class="font-medium text-gray-700">Preencha o questionário completo</h3>
            <p class="text-gray-600 text-sm">Isso nos ajudará a entender melhor seus objetivos e desafios</p>
          </div>
        </div>
        
        <div class="flex items-start">
          <div class="flex-shrink-0 w-8 h-8 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center text-sm font-medium mr-3">3</div>
          <div>
            <h3 class="font-medium text-gray-700">Agende sua consulta estratégica</h3>
            <p class="text-gray-600 text-sm">Após o questionário, você terá acesso ao meu calendário</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Amanda Score Display (if available) -->
    <div id="amanda-score-section" class="bg-purple-50 rounded-lg p-6 mb-8 hidden">
      <h3 class="text-lg font-semibold text-purple-900 mb-2">Seu Perfil Amanda</h3>
      <div class="flex items-center justify-center mb-3">
        <div class="text-3xl font-bold text-purple-600" id="amanda-score-display">--</div>
        <div class="text-sm text-purple-600 ml-2">/ 100</div>
      </div>
      <p class="text-purple-700 text-sm" id="amanda-score-message">
        <!-- Message will be populated by JavaScript -->
      </p>
    </div>

    <!-- Urgency Message -->
    <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-8">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-amber-400" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-amber-800">Atenção importante</h3>
          <div class="mt-1 text-sm text-amber-700">
            <p>O questionário expira em 48 horas. Não perca sua vaga na análise personalizada!</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Social Proof -->
    <div class="border-t pt-8">
      <p class="text-sm text-gray-500 mb-4">Junte-se a centenas de empreendedoras que já transformaram seus negócios</p>
      <div class="flex items-center justify-center space-x-4 text-xs text-gray-400">
        <span>✓ Clínica Estética</span>
        <span>✓ Loja de Roupas</span>
        <span>✓ Nutricionista</span>
        <span>✓ Arquiteta</span>
        <span>✓ E muitas outras</span>
      </div>
    </div>

    <!-- Support Information -->
    <div class="mt-8 pt-6 border-t border-gray-200">
      <p class="text-sm text-gray-600 mb-2">Dúvidas? Precisa de ajuda?</p>
      <a href="https://wa.me/5511999999999?text=Olá! Acabei de me inscrever na Mentoria Seja Livre e tenho algumas dúvidas." 
         class="inline-flex items-center text-green-600 hover:text-green-700 font-medium text-sm"
         target="_blank" rel="noopener noreferrer">
        <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 24 24">
          <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893A11.821 11.821 0 0020.885 3.309"/>
        </svg>
        Falar no WhatsApp
      </a>
    </div>

  </div>
</div>

<script>
  // Thank you page tracking and functionality
  document.addEventListener('DOMContentLoaded', function() {
    // Get URL parameters
    const urlParams = new URLSearchParams(window.location.search);
    const leadId = urlParams.get('lead_id');
    const amandaScore = urlParams.get('amanda_score');
    const sourceSection = urlParams.get('source');

    // Track thank you page view
    if (window.msluIdentityManager) {
      window.msluIdentityManager.pushDataLayerEvent('thank_you_page_view', {
        lead_id: leadId,
        amanda_match_score: amandaScore ? parseInt(amandaScore) : null,
        source_section: sourceSection,
        page_load_timestamp: new Date().toISOString()
      });
    }

    // Display Amanda score if available
    if (amandaScore && parseInt(amandaScore) > 0) {
      displayAmandaScore(parseInt(amandaScore));
    }

    // Animate progress bar
    setTimeout(() => {
      const progressBar = document.getElementById('progress-bar');
      const progressPercentage = document.getElementById('progress-percentage');
      
      progressBar.style.width = '80%';
      progressPercentage.textContent = '80% completo';
    }, 500);

    // Set up email reminder
    setTimeout(() => {
      showEmailReminder();
    }, 300000); // 5 minutes

    // Track questionnaire email sent (server-side trigger simulation)
    if (leadId) {
      setTimeout(() => {
        if (window.msluEmailTracking) {
          window.msluEmailTracking.trackQuestionnaireEmailSent(leadId, 'questionnaire_initial');
        }
      }, 2000);
    }
  });

  function displayAmandaScore(score) {
    const scoreSection = document.getElementById('amanda-score-section');
    const scoreDisplay = document.getElementById('amanda-score-display');
    const scoreMessage = document.getElementById('amanda-score-message');

    scoreDisplay.textContent = score;
    
    let message = '';
    if (score >= 80) {
      message = 'Perfeito! Você tem um perfil ideal para a Mentoria Seja Livre. Aguarde contato prioritário!';
      scoreSection.className = 'bg-green-50 rounded-lg p-6 mb-8';
      scoreDisplay.className = 'text-3xl font-bold text-green-600';
    } else if (score >= 60) {
      message = 'Ótimo! Você se qualifica para a mentoria. Complete o questionário para prosseguir.';
      scoreSection.className = 'bg-blue-50 rounded-lg p-6 mb-8';
      scoreDisplay.className = 'text-3xl font-bold text-blue-600';
    } else {
      message = 'Obrigada pelo interesse! Enviaremos conteúdo especial para seu perfil.';
      scoreSection.className = 'bg-purple-50 rounded-lg p-6 mb-8';
      scoreDisplay.className = 'text-3xl font-bold text-purple-600';
    }
    
    scoreMessage.textContent = message;
    scoreSection.classList.remove('hidden');
  }

  function showEmailReminder() {
    // Show a gentle reminder about checking email
    const reminder = document.createElement('div');
    reminder.className = 'fixed bottom-4 right-4 bg-blue-600 text-white p-4 rounded-lg shadow-lg max-w-sm z-50';
    reminder.innerHTML = `
      <div class="flex items-center">
        <svg class="w-5 h-5 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
        </svg>
        <div>
          <p class="font-medium text-sm">Lembre-se!</p>
          <p class="text-xs">Confira seu email para o questionário</p>
        </div>
        <button onclick="this.parentElement.parentElement.remove()" class="ml-2 text-white hover:text-gray-200">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>
    `;
    
    document.body.appendChild(reminder);
    
    // Auto-remove after 10 seconds
    setTimeout(() => {
      if (reminder.parentElement) {
        reminder.remove();
      }
    }, 10000);
  }

  // Track WhatsApp click
  document.addEventListener('click', function(e) {
    if (e.target.closest('a[href*="wa.me"]')) {
      if (window.msluIdentityManager) {
        window.msluIdentityManager.pushDataLayerEvent('whatsapp_support_click', {
          source_page: 'thank_you',
          click_timestamp: new Date().toISOString()
        });
      }
    }
  });
</script>

</body>
</html>